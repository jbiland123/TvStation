<!DOCTYPE html>

<html lang="de">
   <head>
      <meta charset="UTF-8">
      <meta http-equiv="refresh" content="1800">
      <link href="css/Stylesheet.css" rel="Stylesheet">
      
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <meta http-equiv="X-UA-Compatible" content="ie=edge">
      <link rel="icon" href="Bilder/Logo.png">
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Open+Sans&display=swap" rel="stylesheet">
      <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=DotGothic16">
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
      <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
      <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
      <script src="https://cdn.jsdelivr.net/npm/popper.js@1.14.7/dist/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
      <title>TVStation</title>
   </head>
<body>
    <div class="header">
        <h1>Zentrale Informatik</h1>
        <p class="date" id="current-time"></p>
        <div class="bl-logo">
        <%= image_tag("Baselland.png", size: "235x55") %>
        </div>
    </div>

    <script>
        function updateTime() {
        const currentTime = new Date();
        const options = { weekday: 'long', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' };
        const formattedTime = currentTime.toLocaleDateString('de', options);
        document.getElementById('current-time').innerText = formattedTime;
        }
        updateTime();
        setInterval(updateTime, 1000);
    </script>

    <div class="body">
        <div class="parent">
            <div class="div1"> 
                <div class="inner-parent">
                <div class="div1">
                    <div class="weather">
                        <h2 class="bold">Heute</h2>
                        <img class="weatherImage" src='https://openweathermap.org/img/wn/<%= @data["weather"].first["icon"] %>@2x.png' />
                        <p>Temperatur: <%= @data["main"]["temp"] %>°</p>
                        <p>Gefühlt: <%= @data["main"]["feels_like"] %>°</p>
                        <p>Feuchtigkeit: <%= @data["main"]["humidity"] %>%</p>
                        <i><%= @data["weather"].first["description"] %></i>
                    </div>
                </div>
                <% (2..7).each do |i| %>
                <div class="div<%= i %>">
                    <div class="weatherprediction<%= i > 4 ? 'Under' : '' %>">
                    <p class="bold"><%= german_day_name(Date.parse(@days[i]["datetime"])) %>, <%= german_month_name_short(Date.parse(@days[i]["datetime"])) %> <%= Date.parse(@days[i]["datetime"]).strftime("%d") %></p>
                    <p>Temp: <%= @days[i]["temp"] %>°</p>
                    <i><%= multiline_text(@days[i]["conditions"]) %></i>
                    </div>
                </div>
                <% end %>
            </div>
        </div>
            <div class="div2"> 
                <div class="news">
                    <div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel" data-interval="10000">
                        <ol class="carousel-indicators">
                        <% @i_news.each_with_index do |_, index| %>
                            <li data-target="#carouselExampleIndicators" data-slide-to="<%= index %>" <%= index == 0 ? 'class="active"' : '' %>></li>
                        <% end %>
                        </ol>
                        <div class="carousel-inner">
                            <% @i_news.each_with_index do |item, index| %>
                                <div class="carousel-item <%= index == 0 ? 'active' : '' %>">
                                    <div class="news-slide">
                                        <img class="minheightimg" src="<%= asset_path(item["author"] + ".png") %>" alt="">
                                        <p id="devider">- - - - - - - - - - - - - - - - - - - - - - - - - - - -</p>
                                        <h3 class="minheight"><%= truncate_title(item["title"], item["author"]) %></h3>
                                    </div>
                                </div>
                            <% end %>
                        </div>
                    </div>
                </div>
            </div>
            <div class="div3">
                <div class="calendar">
                    <% status = "Frei" %>
                    <% @events.each do |event| %>
                        <% if event.dtstart <= Time.now && Time.now <= event.dtend %>
                            <% status = "Besetzt" %>
                        <% end %>
                    <% end %>
                    <div class="title">
                        <h2 class="bold">ZZZFKD Sitzungszimmer 310, 3. Stock</h2>
                        <% if status == "Besetzt" %>
                            <img class="size" src="<%= asset_path("occupied.png") %>" alt="">
                        <% else %>
                            <img class="size" src="<%= asset_path("free.png") %>" alt="">
                        <% end %>
                    </div>
                    <% @events.first(5).each do |event| %>
                        <div class="calendar-content">
                            <p class="left max-characters"><%= event.summary %></p>
                            <p class="right"><%= german_month_name(event.dtstart) %> <%= event.dtstart.strftime("%d, %H:%M") %> - <%= event.dtend.strftime("%H:%M") %></p>
                        </div>
                        <p></p>
                    <% end %>
                </div>
            </div>
            <div class="div4">
                <div class="calendar">
                <% status2 = "Frei" %>
                    <% @events2.each do |event2| %>
                        <% if event2.dtstart <= Time.now && Time.now <= event2.dtend %>
                            <% status2 = "Besetzt" %>
                        <% end %>
                    <% end %>
                    <div class="title">
                        <h2 class="bold">ZZZSID Schulungsraum 1, 3. Stock</h2>
                        <% if status2 == "Besetzt" %>
                            <img class="size" src="<%= asset_path("occupied.png") %>" alt="">
                        <% else %>
                            <img class="size" src="<%= asset_path("free.png") %>" alt="">
                        <% end %>
                    </div>
                <% @events2.first(5).each do |event2| %>
                    <div class="calendar-content">
                        <p class="left max-characters"><%= event2.summary %></p>
                        <p class="right"><%= german_month_name(event2.dtstart) %> <%= event2.dtstart.strftime("%d, %H:%M") %> - <%= event2.dtend.strftime("%H:%M") %></p>
                    </div>
                    <p></p>
                <% end %>
                </div>
            </div>
            <div class="div5">
                <div class="message">
                    <marquee behavior="scroll" direction="left" scrollamount="15">
                    <% current_hour = Time.now.hour %>
                    <% if status2 == "Besetzt" %>
                        Herzlich Willkommen an der Frenkendörferstrasse 17 in Liestal! Für die Theorieprüfung warten sie bitte hier. Sie werden bald abgehohlt.
                    <% elsif (6..8).include?(current_hour) %>
                        Guten Morgen und herzlich Willkommen an der Frenkendörferstrasse 17 in Liestal!
                    <% elsif (11..13).include?(current_hour) %>
                        Guten Appetit und bis später! Ich hoffe das Essen dir schmecken.
                    <% elsif (16..20).include?(current_hour) %>
                        Einen schönen Abend und bis bald! Geniesse die Arbeitsfreie Zeit.
                    <% else %>
                        Herzlich Willkommen an der Frenkendörferstrasse 17 in Liestal!
                    <% end %>
                    </marquee>
                </div>
                <div class="progress" role="progressbar" aria-label="Fortschritt des Tages" aria-valuemin="0" aria-valuemax="100">
                    <div class="progress-bar" id="daily-progress" style="width: 0;"></div>
                </div>

                <script>
                function updateProgress() {
                    const now = new Date();
                    const startOfDay = new Date(now);
                    startOfDay.setHours(8, 0, 0, 0); // Setze die Startzeit auf 8 Uhr
                    const endOfDay = new Date(now);
                    endOfDay.setHours(16, 0, 0, 0); // Setze die Endzeit auf 16 Uhr

                    // Berechne den Fortschritt in Prozent
                    const totalMilliseconds = endOfDay - startOfDay;
                    const currentMilliseconds = now - startOfDay;
                    const progressPercent = (currentMilliseconds / totalMilliseconds) * 100;

                    // Setze die Breite der Fortschrittsleiste entsprechend
                    const progressBar = document.getElementById("daily-progress");
                    progressBar.style.width = progressPercent + "%";
                }

                // Rufe die Funktion zum Aktualisieren alle 10 Sekunden auf (optional kannst du das Intervall anpassen)
                setInterval(updateProgress, 10000);
                </script>


            </div>
        </div>
    </div>
   </body>
</html>